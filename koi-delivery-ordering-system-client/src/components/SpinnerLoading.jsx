// Spinner.js
import './style/spinner.scss'; // Import the CSS for the spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
